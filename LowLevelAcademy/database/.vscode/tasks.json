{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-debug",
            "type": "shell",
            "command": "mkdir -p bin obj && make CFLAGS='-g -std=c90 -O0'",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}